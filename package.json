{
  "name": "ebsecurity",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc && node scripts/copy-static.mjs",
    "gitupdate": "git add . && git commit -m \"Atualização automática\" && git push",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2",
    "fs": "^0.0.1-security",
    "readline": "^1.3.0",
    "rozod": "^6.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "rimraf": "^6.0.1",
    "typescript": "^7.0.2"
  }
}